## OnlyClass LMS - Environment variables (example)
## Create your real .env in production (DO NOT commit)

NODE_ENV=development
PORT=3000

## MongoDB Atlas connection string
## Example:
## mongodb+srv://onlyclass_user:YOUR_PASSWORD@cluster0.xxxxx.mongodb.net/onlyclass?retryWrites=true&w=majority
MONGODB_URI=

## JWT secrets (generate long random strings)
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=

## Admin seed
## If ADMIN_PASSWORD is empty, dev fallback may be used (not recommended for production)
ADMIN_EMAIL=admin@onlyclass.local
ADMIN_PASSWORD=

## Optional: seed demo users/tracks (development only recommended)
SEED_DEMO=true

## If frontend is served from a different origin (optional)
## Example: https://onlyclass.uz
APP_ORIGIN=

